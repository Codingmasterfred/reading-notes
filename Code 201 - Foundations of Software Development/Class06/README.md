

### How would you describe an object to a non-technical friend you grew up with?
an object is a way to store imformation on anything. For example a pencil is a object. We can store information pertaining to the pencil, such as it color, size, and weight.

### What are some advantages to creating object literals?
It can make your code look cleaner and mor organize. It also can help you reframe from re-entering code by utilizing methods and properties.

### How do objects differ from arrays?
objects used curly brackets and meant to store information to and object, where as, arrays is mostly used in an orderly fasion and uses square brackets.

### Give an example for when you would need to use bracket notation to access an object’s property instead of dot notation.
Dot notation uses a period in between the object and property to find the value you are looking for while bracket notation is finding an item using the object next to the property suurounded by quotes and curly brackets.

### Evaluate the code below. What does the term this refer to and what is the advantage to using this?
This is just an replacement for person. You use it when creating  single object literal.

### What is the DOM?
Dom, Document Object Model , is a progromming interface that allow programs to change the webpage. 


### Briefly describe the relationship between the DOM and JavaScript.
The Document Object Model (DOM) is a programming interface that allows JavaScript to access and manipulate the content and structure of HTML document. In other words, the DOM provides a way for JavaScript to interact with the elements on a webpage, such as modifying text, adding or removing elements, and changing styles. JavaScript can use the DOM to dynamically update the content of a webpage based on user input or other events.